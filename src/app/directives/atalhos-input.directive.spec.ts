import { AtalhosInputDirective } from './atalhos-input.directive';

describe('AtalhosInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AtalhosInputDirective();
    expect(directive).toBeTruthy();
  });
});
